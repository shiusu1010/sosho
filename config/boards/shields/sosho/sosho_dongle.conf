# --- 画面表示の有効化 ---
CONFIG_ZMK_DISPLAY=y

# --- 入力機能 ---
CONFIG_INPUT=y
CONFIG_ZMK_POINTING=y

# --- HID（PCへのマウス通知）の有効化 ---
CONFIG_ZMK_MOUSE=y

# --- 通信安定化 ---
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y

# ==========================================
# Prospector 追加ディスプレイ機能
# ==========================================

# --- リアルタイム打鍵速度（WPM）の有効化 ---
CONFIG_ZMK_WPM=y