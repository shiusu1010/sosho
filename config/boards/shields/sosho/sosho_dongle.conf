# --- 画面表示はとりあえずオフ ---
CONFIG_ZMK_DISPLAY=n

# --- 入力機能 ---
CONFIG_INPUT=y
CONFIG_ZMK_POINTING=y

# (重要: ZMK_SPLIT_POINTING は環境によってはエラーになるため削除しました)

# --- HID（PCへのマウス通知）の有効化 ---
CONFIG_ZMK_MOUSE=y

# --- 通信安定化 ---
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y