# --- 画面表示機能 ---
# 画面表示を有効化
CONFIG_ZMK_DISPLAY=y

# --- 入力機能 (重要: これがないとトラックボール/パッドが反応しません) ---
# 入力処理とポインティングデバイスの有効化
CONFIG_INPUT=y
CONFIG_ZMK_POINTING=y

# 分割キーボード間で入力データを同期させる設定
CONFIG_ZMK_SPLIT_POINTING=y

# --- 通信安定化 ---
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y