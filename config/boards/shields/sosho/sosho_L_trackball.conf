# --- 識別名 ---
CONFIG_ZMK_KEYBOARD_NAME="sosho-L"

# --- 通信・電力設定 ---
CONFIG_ZMK_BLE=y
# 【変更】左手もスリープ機能を完全にオフ（常に浅い待機モードを維持）
CONFIG_ZMK_SLEEP=n
CONFIG_ZMK_BATTERY_REPORTING=y
CONFIG_BT_BAS=y

# --- ポインティングデバイス設定（ドライバ復活！） ---
CONFIG_INPUT=y
CONFIG_SPI=y
CONFIG_INPUT_INIT_PRIORITY=99
CONFIG_INPUT_PMW3610=y
CONFIG_ZMK_POINTING=y

# --- ロータリーエンコーダー設定 ---
CONFIG_EC11=y
CONFIG_EC11_TRIGGER_GLOBAL_THREAD=y
CONFIG_ZMK_BEHAVIORS_QUEUE_SIZE=128

# --- 通信の安定化 ---
CONFIG_BT_CTLR_TX_PWR_PLUS_8=n

# --- 処理能力の確保 ---
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=2048

# --- 特殊ピン設定 ---
CONFIG_NFCT_PINS_AS_GPIOS=y

# ========================================================================
# 🔋 バッテリー対策：LEDシステムの完全無効化
# ========================================================================
# メインのスイッチを「n」にして機能をオフにします
CONFIG_RGBLED_WIDGET=n

# 以下の2行は、後でLEDを復活させたくなった時のために
# 「#」をつけてメモとして残しておきます（システムからは無視されます）
# CONFIG_RGBLED_WIDGET_BATTERY_LEVEL_HIGH=30
# CONFIG_RGBLED_WIDGET_BATTERY_LEVEL_CRITICAL=10